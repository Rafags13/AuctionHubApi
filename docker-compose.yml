version: '3.8'
services:
  auctionhub.webapi:
    image: ${DOCKER_REGISTRY-}auctionhubwebapi
    build:
      context: .
      dockerfile: AuctionHub.WebApi/Dockerfile
    depends_on:
      - db

  db:
    image: postgres:18.3
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data: